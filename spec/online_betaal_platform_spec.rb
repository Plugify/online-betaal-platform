RSpec.describe OnlineBetaalPlatform do
  it "has a version number" do
    expect(OnlineBetaalPlatform::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
